# Core
streamlit>=1.36
numpy>=1.23
pandas>=1.5
matplotlib>=3.7
pillow>=10.0
pymupdf

# Mapping
pydeck>=0.8   # only if you're keeping the map pages

# Optimization / GA demos
pymoo>=0.6

# Machine learning (used in Keystone Species demo / ML bits)
scikit-learn>=1.3
xgboost>=1.7

# Geospatial (you’ve shown geopandas/QGIS workflows in portfolio)
geopandas>=0.14
shapely>=2.0
fiona>=1.9

# PDF handling (for the page-trimming + embed demos)
pypdf2>=3.0
